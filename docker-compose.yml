services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev # Explicitly targets the development stage
    container_name: tsizehena-portfolio
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.astro
    environment:
      - NODE_ENV=development